let count1 = 0 
let count2 = 0 
let count3 = 0 
let count4 = 0 
let count5 = 0 
let foodArr = 0 
let rentArr = 0 
let transArr = 0 
let miscArr = 0 
let savArr = 0 
function removeElement1(e) { 
count1--; 
if (count1 == 0) { 
document.getElementById("save1").style.display = 'none'; } 
let button = e.target; 
let field = button.previousSibling; 
let div = button.parentElement; 
let br = button.nextSibling; 
div.removeChild(button); 
div.removeChild(field); 
div.removeChild(br); 
let allElements = document.getElementById("reqs1"); 
let inputs = allElements.getElementsByTagName("input"); for(i=0;i<inputs.length;i++){ 
inputs[i].setAttribute('id', 'reqs1' + (i+1)); 
inputs[i].nextSibling.setAttribute('id', 'reqsr1' + (i+1)); 
} 
} 
function add1() { 
if (document.getElementById("save1").style.display == 'none' ) { document.getElementById("save1").style.display = 'block'; } 
let allElements = document.getElementById("reqs1"); 
let reqs_id = allElements.getElementsByTagName("input").length; 
reqs_id++; 
count1 = reqs_id 
//create textbox 
let input = document.createElement('input'); 
input.type = "text";
input.setAttribute("class", "w3-input w3-border in1"); 
input.setAttribute('id', 'reqs1' + reqs_id); 
input.setAttribute('placeholder' , 'type expense here') 
let reqs = document.getElementById("reqs1"); 
//create remove button 
let remove = document.createElement('button'); 
remove.setAttribute('id', 'reqsr1' + reqs_id); 
remove.onclick = function(e) { 
removeElement1(e); 
}; 
remove.setAttribute("class", "buttonsecondary"); 
remove.innerHTML = "Remove"; 
//append elements 
reqs1.appendChild(input); 
reqs1.appendChild(remove); 
let br = document.createElement("br"); 
reqs1.appendChild(br); 
} 
/*btn 2*/ 
function removeElement2(e) { 
count2--; 
if (count2 == 0) { 
document.getElementById("save2").style.display = 'none'; } 
let button = e.target; 
let field = button.previousSibling; 
let div = button.parentElement; 
let br = button.nextSibling; 
div.removeChild(button); 
div.removeChild(field); 
div.removeChild(br); 
let allElements = document.getElementById("reqs2"); 
let inputs = allElements.getElementsByTagName("input"); for(i=0;i<inputs.length;i++){
inputs[i].setAttribute('id', 'reqs2' + (i+1)); 
inputs[i].nextSibling.setAttribute('id', 'reqsr2' + (i+1)); 
} 
} 
function add2() { 
if (document.getElementById("save2").style.display == 'none' ) { document.getElementById("save2").style.display = 'block'; } 
count2++; 
let allElements = document.getElementById("reqs2"); let reqs_id = allElements.getElementsByTagName("input").length; 
reqs_id++; 
//create textbox 
let input = document.createElement('input'); 
input.type = "text"; 
input.setAttribute("class", "w3-input w3-border in2"); 
input.setAttribute('id', 'reqs2' + reqs_id); 
input.setAttribute('placeholder' , 'type expense here') 
let reqs = document.getElementById("reqs2"); 
//create remove button 
let remove = document.createElement('button'); 
remove.setAttribute('id', 'reqsr2' + reqs_id); 
remove.onclick = function(e) { 
removeElement2(e); 
}; 
remove.setAttribute("class", "buttonsecondary"); 
remove.innerHTML = "Remove"; 
//append elements 
reqs2.appendChild(input); 
reqs2.appendChild(remove); 
let br = document.createElement("br"); 
reqs2.appendChild(br); 
} 
/*btn 3*/ 
function removeElement3(e) {
count3--; 
if (count3 == 0) { 
document.getElementById("save3").style.display = 'none'; } 
let button = e.target; 
let field = button.previousSibling; 
let div = button.parentElement; 
let br = button.nextSibling; 
div.removeChild(button); 
div.removeChild(field); 
div.removeChild(br); 
let allElements = document.getElementById("reqs3"); 
let inputs = allElements.getElementsByTagName("input"); for(i=0;i<inputs.length;i++){ 
inputs[i].setAttribute('id', 'reqs3' + (i+1)); 
inputs[i].nextSibling.setAttribute('id', 'reqsr3' + (i+1)); 
} 
} 
function add3() { 
if (document.getElementById("save3").style.display == 'none' ) { document.getElementById("save3").style.display = 'block'; } 
count3++; 
let allElements = document.getElementById("reqs3"); 
let reqs_id = allElements.getElementsByTagName("input").length; reqs_id++; 
//create textbox 
let input = document.createElement('input'); 
input.type = "text"; 
input.setAttribute("class", "w3-input w3-border in3"); 
input.setAttribute('id', 'reqs3' + reqs_id); 
input.setAttribute('placeholder' , 'type expense here'); 
let reqs = document.getElementById("reqs3"); 
//create remove button 
let remove = document.createElement('button'); 
remove.setAttribute('id', 'reqsr3' + reqs_id); 
remove.onclick = function(e) { 
removeElement3(e); 
}; 
remove.setAttribute("class", "buttonsecondary");
remove.innerHTML = "Remove"; 
//append elements 
reqs3.appendChild(input); 
reqs3.appendChild(remove); 
let br = document.createElement("br"); 
reqs3.appendChild(br); 
} 
/*btn 4*/ 
function removeElement4(e) { 
count4--; 
if (count4 == 0) { 
document.getElementById("save4").style.display = 'none'; } 
let button = e.target; 
let field = button.previousSibling; 
let div = button.parentElement; 
let br = button.nextSibling; 
div.removeChild(button); 
div.removeChild(field); 
div.removeChild(br); 
let allElements = document.getElementById("reqs4"); 
let inputs = allElements.getElementsByTagName("input"); for(i=0;i<inputs.length;i++){ 
inputs[i].setAttribute('id', 'reqs4' + (i+1)); 
inputs[i].nextSibling.setAttribute('id', 'reqsr4' + (i+1)); 
} 
} 
function add4() { 
if (document.getElementById("save4").style.display == 'none' ) { document.getElementById("save4").style.display = 'block'; } 
count4++; 
let allElements = document.getElementById("reqs4"); 
let reqs_id = allElements.getElementsByTagName("input").length; reqs_id++; 
//create textbox 
let input = document.createElement('input'); 
input.type = "text";
input.setAttribute("class", "w3-input w3-border in4"); 
input.setAttribute('id', 'reqs4' + reqs_id); 
input.setAttribute('placeholder' , 'type expense here'); 
let reqs = document.getElementById("reqs4"); 
//create remove button 
let remove = document.createElement('button'); 
remove.setAttribute('id', 'reqsr4' + reqs_id); 
remove.onclick = function(e) { 
removeElement4(e); 
}; 
remove.setAttribute("class", "buttonsecondary"); 
remove.innerHTML = "Remove"; 
//append elements 
reqs4.appendChild(input); 
reqs4.appendChild(remove); 
let br = document.createElement("br"); 
reqs4.appendChild(br); 
} 
/*btn 5*/ 
function removeElement5(e) { 
count5--; 
if (count5 == 0) { 
document.getElementById("save5").style.display = 'none'; } 
let button = e.target; 
let field = button.previousSibling; 
let div = button.parentElement; 
let br = button.nextSibling; 
div.removeChild(button); 
div.removeChild(field); 
div.removeChild(br); 
let allElements = document.getElementById("reqs5"); 
let inputs = allElements.getElementsByTagName("input"); for(i=0;i<inputs.length;i++){ 
inputs[i].setAttribute('id', 'reqs5' + (i+1));
inputs[i].nextSibling.setAttribute('id', 'reqsr5' + (i+1)); 
} 
} 
function add5() { 
if (document.getElementById("save5").style.display == 'none' ) { document.getElementById("save5").style.display = 'block'; } 
count5++; 
let allElements = document.getElementById("reqs5"); 
let reqs_id = allElements.getElementsByTagName("input").length; reqs_id++; 
//create textbox 
let input = document.createElement('input'); 
input.type = "text"; 
input.setAttribute("class", "w3-input w3-border in5"); 
input.setAttribute('id', 'reqs5' + reqs_id); 
input.setAttribute('placeholder' , 'type expense here'); 
let reqs = document.getElementById("reqs5"); 
//create remove button 
let remove = document.createElement('button'); 
remove.setAttribute('id', 'reqsr5' + reqs_id); 
remove.onclick = function(e) { 
removeElement5(e); 
}; 
remove.setAttribute("class", "buttonsecondary"); 
remove.innerHTML = "Remove"; 
//append elements 
reqs5.appendChild(input); 
reqs5.appendChild(remove); 
let br = document.createElement("br"); 
reqs5.appendChild(br); 
} 
function Save1() { 
var cou = 0 
for (i=1; i <= count1; i++) {
let ele = parseInt(document.getElementById('reqs' + i).value) cou = cou + Number(ele) 
} 
foodArr = cou 
alert(foodArr) 
} 
function Save2() { 
for (i=0; i < count2; i++) { 
rentArr += parseInt(document.getElementById('reqs2' + i).value) } 
alert(rentArr) 
} 
function Generate(){ 
}
